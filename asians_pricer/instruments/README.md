# instruments

Contract definitions.

- `asian_option.py`: `AsianOption(strike, maturity, is_call=True, notional=1.0, averaging_observations=None)` with `payoff(average)` helper.

Used as the payoff input to pricing engines.
